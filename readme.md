# parolee

Fork of [SogoCZE/Jails](https://github.com/SogoCZE/Jails). The reason this fork exists is my need to store this somewhere, but I do not want to PR vibecoded pieces of code I never checked. This repo is 

## Features

- **Semantic syntax highlighting** — full LSP semantic tokens provider with delta updates.
- **Autocomplete** — symbols, types, procedures, variables, keywords. Even for generated types.
